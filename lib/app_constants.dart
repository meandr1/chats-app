import 'package:flutter/material.dart';
export 'package:chats/app_constants.dart';

// App colors
const Color textFormFieldColor = Color.fromARGB(255, 50, 74, 138);
const Color textButtonColor = Color.fromARGB(255, 50, 74, 138);
const Color elevatedButtonColor = Color.fromARGB(255, 98, 121, 183);
const Color iconsColor = Color.fromARGB(255, 98, 121, 183);
const Color appBarColor = Color.fromARGB(255, 139, 161, 221);
const Color bottomNavigationBarColor = Color.fromARGB(255, 98, 121, 183);

// auth cubit constants
const String unknownError = 'An unknown error occurred';

// Max shown message length on main screen
const int maxShownMessageLength = 80;

// The diameter of avatar images
const double imageDiameterSmall = 50.0;

// The diameter of personal profile avatar image
const double imageDiameterLarge = 150.0;

// Default icon of profile without photo
const IconData defaultPersonIcon = Icons.person;

// Max shown message length on main screen
const double defaultButtonHigh = 40;